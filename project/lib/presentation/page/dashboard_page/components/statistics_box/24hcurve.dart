part of '../../page.dart';

class H_Curve extends StatelessWidget {
  const H_Curve();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('24H CURVE'),
    );
  }
}
